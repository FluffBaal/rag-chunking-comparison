# Minimal requirements for Vercel deployment
# Core dependencies only - additional packages can be installed at runtime

# Essential text processing
nltk>=3.9.1
tiktoken>=0.8.0

# Basic ML/NLP without heavy dependencies
numpy>=1.26.4
scikit-learn>=1.5.2
pandas>=2.2.3

# API dependencies
openai>=1.54.4

# PDF processing
pypdf2>=3.0.1

# Note: Heavy dependencies like torch, transformers, sentence-transformers
# are commented out to speed up deployment. They can be installed on-demand
# or you can use a custom runtime.