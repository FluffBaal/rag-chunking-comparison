# Lightweight requirements for full functionality on Vercel
# Uses API-based solutions instead of heavy ML libraries

# Core dependencies
numpy>=1.26.4
scikit-learn>=1.5.2

# Text processing
nltk>=3.9.1
tiktoken>=0.8.0

# API clients
openai>=1.54.4

# PDF processing (lightweight)
pypdf2>=3.0.1

# For RAGAS metrics (lightweight version)
# We'll implement our own RAGAS calculations
pandas>=2.2.3

# Total size: ~50MB (vs 2GB+ with torch/transformers)